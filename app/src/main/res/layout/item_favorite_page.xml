<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="10dp"
    android:layout_marginVertical="4dp"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground"
    app:cardUseCompatPadding="true"
    app:cardCornerRadius="12dp"
    app:cardElevation="1.5dp"
    app:cardBackgroundColor="@color/favCardBg"
    app:strokeColor="@color/favCardStroke"
    app:strokeWidth="0.8dp"
    app:rippleColor="@color/favCardRipple">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingHorizontal="10dp"
        android:paddingVertical="6dp">

        <!-- العنوان -->
        <TextView
            android:id="@+id/bookmarkTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:maxLines="1"
            android:ellipsize="end"
            android:textStyle="bold"
            android:textSize="12sp"
            android:textColor="@color/bookmarkTitleColor"
            android:text="@string/click_to_open_page" />

        <!-- التلميح -->
        <TextView
            android:id="@+id/bookmarkHint"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="6dp"
            android:maxLines="1"
            android:textSize="12sp"
            android:textColor="@color/textSecondary"
            android:text="@string/tap_to_open" />

        <!-- زر الحذف -->
        <ImageButton
            android:id="@+id/deleteIcon"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:layout_marginStart="4dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/delete"
            app:srcCompat="@drawable/ic_delete"
            app:tint="@color/deleteRed" />
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
