<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/btn_primary_bg">

    <!-- شريط التبويب بلون متطابق مع بقية الأشرطة -->
    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/colorBackground"
        android:elevation="0dp"
        android:stateListAnimator="@null">

        <!-- نجعل اللون الحقيقي هنا لضمان ظهوره فوق أي surface من الثيم -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="#f4ecdc">

            <com.google.android.material.tabs.TabLayout
                android:id="@+id/tabLayout"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:tabMode="fixed"
                app:tabGravity="fill"
                app:tabIndicatorFullWidth="false"
                app:tabIndicatorHeight="3dp"
                android:background="@color/tabBackground"
                app:tabIndicatorColor="@color/tabIndicator"
                app:tabSelectedTextColor="@color/tabTextSelected"
                app:tabTextColor="@color/tabTextUnselected"
                app:tabRippleColor="@android:color/transparent"
                app:tabTextAppearance="@style/TabText.Bold" />
        </LinearLayout>
    </com.google.android.material.appbar.AppBarLayout>

    <!-- محتوى الصفحات -->
    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/viewPager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/colorBackground"
        app:layout_behavior="@string/appbar_scrolling_view_behavior" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
